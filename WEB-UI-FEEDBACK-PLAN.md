# Web UI 回饋修正規劃（第一輪）

> 狀態：規劃完成、尚未實作。2026-08-04 依實測回饋二項。
> 兩項皆純前端（chat.css / chat.js），後端、DB、既有 136 條測試零影響。

---

## 問題一：字體太小，且字級切換只影響泡泡

### 根因

- 現行三檔字級（小 .8rem／中 .95rem／大 1.15rem）只透過 `--chat-font-size` 套用在 **`.bubble` 一個元素**上。
- 頁面其他所有文字——傳送者名字（.75rem）、時間戳（.65rem）、側欄群組名（.92rem）、預覽（.8rem）、標頭（1rem）、日期分隔（.75rem）、底部列（.78rem）——全是**固定值**，切字級完全不動。
- 「大」也只有 1.15rem，整體基準偏小。

### 方案：基準值放大 + 單一縮放係數全頁生效

1. **重新定基準（= 新的「小」）**：以現行「大」為新的最小檔。聊天頁內所有文字
   等比放大約 1.2 倍作為新基準：

   | 元素 | 現行 | 新基準（小） |
   |---|---|---|
   | 泡泡內文 | .95rem（中） | **1.15rem** |
   | 傳送者名字 | .75rem | .9rem |
   | 時間戳 | .65rem | .8rem |
   | 日期分隔 | .75rem | .9rem |
   | 側欄群組名 | .92rem | 1.1rem |
   | 側欄預覽 | .8rem | .95rem |
   | 側欄時間 | .7rem | .85rem |
   | 側欄標題 | 1.05rem | 1.25rem |
   | 聊天標頭名稱 | 1rem | 1.2rem |
   | 標頭成員數 | .78rem | .95rem |
   | 底部列膠囊 | .78rem | .95rem |
   | 載入更早膠囊 | .75rem | .9rem |
   | 連線橫幅 | .8rem | .95rem |
   | 設定入口 | .85rem | 1rem |

2. **切換機制改為縮放係數**：`.chat-app { --font-scale: 1 }`（小）、
   `.font-size-medium { --font-scale: 1.15 }`、`.font-size-large { --font-scale: 1.3 }`；
   chat.css 內上表所有 `font-size` 改寫成 `calc(<基準>rem * var(--font-scale))`。
   → 泡泡實際尺寸：小 1.15rem／中 ≈1.32rem／大 ≈1.5rem，且**整頁所有文字**跟著三檔縮放。

3. **不變的部分**：
   - class 名（`font-size-small/medium/large`）與 localStorage key 照舊 → **chat.js 零改動**，
     使用者既有偏好直接沿用（原本存「大」的人會拿到新的最大檔，方向符合預期）。
   - 頭貼、頭貼內 emoji 圖示尺寸固定不縮放（圖形元素，跟著文字縮放反而怪）。
   - 圖片/影片最大尺寸不變。

4. **版面連動處理**：
   - `message-list` 頂端的 `padding-top: 2.6rem`（讓開載入更早膠囊）改成
     `calc()` 跟著膠囊字級長高，避免大字級時膠囊蓋到第一則訊息。
   - 標頭／側欄項目高度是彈性布局，自然長高，不需硬編碼調整。
   - 手機版（375px）需實測「大」檔side欄項目與標頭不換行破版（名稱本來就有 ellipsis）。

5. **範圍界定**：字級切換是聊天頁的功能（設定存 localStorage、只有 chat.js 讀），
   所以「整個頁面」= 聊天頁整頁（側欄+標頭+訊息區+底部列）。
   設定頁維持 Bootstrap 預設字級不受影響——若設定頁也要放大再另議（不建議綁同一顆開關）。

---

## 問題二：在第二個群組發言，側欄沒有跳出新群組

### 根因（已用 log + DB 實查證實，不是收錄端問題）

- 收錄端 log：`Saved text message ... from group C312f...` → 新群組訊息**有收到、有寫入**。
- DB 實查：兩個群組都在 `GroupMessages`，新群組的 `Groups` 名稱快取與 `GroupMembers` 也都正常。
- 真正原因：chat.js 的 `loadGroups()` **只在頁面載入時執行一次**；之後的 4 秒輪詢
  （`pollNewer()`）只抓「目前選取群組」的新訊息。側欄從不重新抓取 →
  新群組、最後訊息預覽、時間、排序、成員數全部凍結在頁面載入當下，重新整理才會出現。
  （改版體檢時已把「側欄預覽不會即時更新」記為已知限制，這次實測確認需要補上。）

### 方案：側欄群組輪詢

1. 新增獨立輪詢 `pollGroups()`：`setInterval` 每 **15 秒**呼叫 `GET /api/groups`，
   成功後整批替換 `state.groups` 並 `renderGroupList(目前搜尋字)` 重繪。
   - 15 秒的理由：側欄資訊（新群組/預覽/排序）不需要 4 秒級即時性；
     `/api/groups` 一次要跑 4 個查詢＋載入遮蔽規則，沒必要跟訊息輪詢同頻。
2. 重繪時保留使用者狀態：
   - 搜尋過濾字沿用（新群組若不符過濾條件就照常被濾掉）。
   - 選取態沿用（`createGroupItem` 本來就依 `state.groupId` 標 active）。
   - **保留列表 scrollTop**（先記再重繪再還原），避免使用者正在捲列表時被跳回頂端。
3. 防護 guards（比照 `pollNewer`）：`document.hidden` 時跳過、in-flight 旗標防重疊。
4. 錯誤處理：**失敗靜默跳過**，不接 `setConnectionOk`——連線狀態已由訊息輪詢負責，
   兩條輪詢各自回報會在「一好一壞」的交錯下讓橫幅閃爍。
   例外：`state.groupId === null`（空庫啟動、還沒有任何群組）時，群組輪詢是唯一的
   對外請求，此情況下才回報連線狀態。
5. 附帶修掉一個隱性問題：目前空庫啟動後側欄永遠停在「尚無群組資料」，
   即使之後有訊息進來也不會出現。改為：輪詢發現群組且 `state.groupId === null` 時
   **自動選取第一個群組**（= 最新活動的群組）。
6. 伺服器負載評估：單一使用者、每 15 秒一次唯讀查詢（群組數個位數），可忽略；
   `/api/groups` 不寫任何資料（代號指派只在訊息端點），無副作用。

### 刻意不做（範圍外）

- **每群組未讀數 badge**：需要「已讀」狀態追蹤，目前架構沒有這個概念，另案。
- **目前群組的預覽即時更新**（不等 15 秒）：要在前端複製 type→label 與遮蔽邏輯
  才能從 `pollNewer` 的訊息就地組預覽，重複邏輯不值得，15 秒內側欄自然更新。

---

## 影響評估

| 面向 | 影響 |
|---|---|
| 後端 / DB / migration | 零改動 |
| 自動化測試（33+103） | 零影響，不需新增（純前端，專案無 JS 測試框架，維持人工驗證） |
| chat.js | 新增 `pollGroups()` 與 interval、scrollTop 保留邏輯；既有 state 機制不動 |
| chat.css | 字級 token 化重寫（上表 14 處 font-size 改 calc）＋ padding 連動一處 |
| 既有使用者偏好 | localStorage 的 small/medium/large 直接沿用，語意不變（相對大小） |

## 人工驗證清單（實作後）

1. 三檔字級：泡泡、名字、時間戳、側欄、標頭、底部列全部跟著縮放；重新整理後偏好保留。
2. 「大」檔在桌面與 375px 手機寬皆不破版（側欄項目、標頭、載入更早膠囊不重疊）。
3. 開著頁面在**另一個群組**發訊息 → 15 秒內側欄自動跳出該群組並排到最上（不用重新整理）。
4. 側欄輪詢期間：搜尋過濾持續生效、選取態不跳、列表捲動位置不被重設。
5. 空資料庫啟動 → 第一則訊息進來後 15 秒內自動選取群組並顯示對話。
6. 既有回歸：切群組防競態、載入更早、置底跟隨/未讀、圖片 modal 照常。
