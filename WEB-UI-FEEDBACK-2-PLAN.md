# Web UI 回饋修正規劃（第二輪）

> 狀態：**已實作完成**（2026-08-04，分支 `feature/web-ui-feedback-2`）。
> 影響範圍：①③④⑥純前端；②前端＋設定頁（不動 DB）；⑤新增後端搜尋 API＋12 條測試（本輪唯一動後端項）。
> 148 測試綠（33+115）。已用瀏覽器端到端驗證全部六項，含跨功能組合（匿名模式＋搜尋、
> 手機版＋搜尋面板、字級 px 設定＋設定頁同步）。
> 回饋①的參考截圖全程未收到，實作依現況版面推斷（側欄/聊天面板左右留白不一致、直角色塊），
> 使用者未在實作前補圖，視為推斷方向已被接受。

---

## ① 框線生硬（直角）＋ 元素沒對齊

### 根因

- **對齊**：側欄各區塊左邊界四種值各自為政——標題 `1rem`、搜尋框 `.75rem`、
  群組項目 `.9rem`、設定入口 `.5+.75=1.25rem`，掃過去就是「沒對齊的感覺」。
  聊天面板同病：標頭 `.9rem`、訊息區 `.75rem`、底部列 `.75rem`。
- **直角**：搜尋輸入框是 Bootstrap 預設小圓角（近直角）；群組項目 hover/選取是
  貼滿整列的直角色塊；連線橫幅、設定入口皆直角。

### 方案

1. 定義版面 token：`--gutter: 1rem`（左右統一留白）、`--radius-sm: .5rem`、`--radius-md: .75rem`。
2. 側欄四區塊（標題／搜尋／群組項目內容／設定入口）與聊天面板三區塊（標頭／訊息區／底部列）
   全部對齊到 `--gutter`。
3. 搜尋框改 LINE 式膠囊：灰底（沿用 `--line-composer-pill`）、無邊框、全圓角，🔍 內嵌。
4. 群組項目 hover/選取色塊改「內縮圓角卡」：左右 margin `.5rem`、`--radius-md` 圓角
   （近代 LINE 桌面版樣式，視覺柔和）；設定入口同款。
5. 連線橫幅改為浮在標頭下緣的圓角 pill（現為全寬直角條）。

---

## ② 字體大小數值化＋設定頁同步

需求拆解：(a) 設定頁新增「字體大小」數值設定（如 14 = 14px）；聊天頁字級「中」= 該數值；
(b) 設定頁自身的文字也要跟著這個設定縮放。

### 方案

1. **模型**：`基準值 basePx`（= 泡泡內文在「中」檔的 px 大小）＋ 既有三檔改為相對「中」的係數：
   小 ×0.87、中 ×1、大 ×1.13（等比於現行 1／1.15／1.3 正規化到中檔）。
   聊天頁 16 處文字全部改寫為 `calc(var(--chat-base) * <元素比例> * var(--font-scale))`，
   元素比例 = 該元素現行基準 ÷ 泡泡基準 1.15rem（例：時間戳 .8/1.15 ≈ .7）。
2. **預設值**：`basePx = 20`（≈ 現行桌面版「中」的實際大小 21px，幾乎無感遷移）。
   可調範圍 clamp 12–28。
3. **儲存**：沿用既有決策——**localStorage、不進 DB**（顯示偏好跟裝置螢幕走，
   每台各自記；`ViewerSettings` 不加欄位、零 migration）。新 key `chat-font-base-px`，
   與既有 `chat-font-size`（小/中/大）並存。設定頁 JS 直接讀寫同一組 key。
4. **設定頁 UI**：新增「介面顯示」卡片：數值輸入（px）＋ 小/中/大快速檔位（與聊天頁 Aa 同步）
   ＋即時預覽文字。變更立即套用（settings 頁自身文字也吃 `--chat-base`×`--font-scale`）。
5. **設定頁自身同步**：settings.js 啟動時讀 localStorage 套用到 `.settings-app`
   （卡片標題、表格、表單控件跟著縮放）。
6. **取捨**：改 px 基準後，原本 html 14px→16px 的響應式基準（<768px 字更小）不再影響聊天字級——
   px 是使用者指定的絕對值，跨裝置一致，這正是需求要的行為；手機嫌大就自己調小。

---

## ③ 字級放大後泡泡小尾巴跑版

### 根因

尾巴是**固定 11px、貼泡泡左下角**的偽元素；字級放大後泡泡圓角（1.1rem ≈ 18px+）
比尾巴還大，尾巴跟圓弧接不起來、看起來脫落；且頭貼是頂端對齊，
尾巴卻在底部，指向不合理（改版時沿用了舊值沒跟著頭貼對齊方式調整）。

### 方案

1. 尾巴移到**左上角**（與頂端對齊的頭貼、名字同側），LINE 實際樣式也是如此。
2. 尺寸改 em 基準（約 `.55em`），跟著字級縮放，不再是固定 px。
3. `has-tail` 泡泡的左上圓角縮小（如 `.3rem`），讓尾巴與角自然銜接。
4. 三檔字級 × 桌面/手機寬各實測一次（此項規劃保留實作時微調空間，以視覺正確為準）。

---

## ④ 群組名稱（聊天標頭）加底色，與對話區分離

### 根因

改版時依 LINE 截圖把標頭做成與聊天區同色（透明感），但實際使用上群組名稱
浮在同色背景上，與對話內容缺乏分層。

### 方案

- 標頭加獨立底色：聊天藍加深一階（約 `#7B9BD2`，token 化為 `--line-header-bg`）
  ＋下緣細陰影（`0 1px 3px rgba(0,0,0,.12)`），與訊息區形成明確分層；
  文字/圖示維持深色，對比足夠。
- 手機版返回鈕、Aa、搜尋鈕同在此底色上，一致處理。
- （備註：若你原意是指「訊息上方的傳送者名字」要加底色籤條，跟我說一聲，
  改為 sender-name 加半透明白底 pill 的方案，成本相同。）

---

## ⑤ 訊息搜尋（本輪最大項，唯一動後端）

需求：搜尋全部群組或單一群組；比對**訊息內容＋使用者名稱**；結果篩選＋高亮；
入口平常收合，放在 Aa 旁邊。

### 後端：新端點 `GET /api/messages/search`

- 參數：`q`（必填，trim 後至少 1 字）、`groupId`（選填，缺省＝全部群組）、上限 100 筆、新→舊排序。
- 回傳：`SearchResultDto[]`：訊息 id、groupId、群組名、解析後的發言者名稱、
  遮蔽後的內容摘要、時間戳。
- **遮蔽一致性（嚴肅環境的核心設計，會寫回歸測試釘死）**：
  1. 內容比對不能讓搜尋變成遮蔽的後門：SQL 先用原文 LIKE 撈候選，
     記憶體內套 `MaskText` 後**重新驗證**遮蔽後文字仍含關鍵字才算命中——
     被關鍵字規則遮掉的詞（如「密碼」）搜不到、摘要也是遮蔽後文字。
  2. 名稱比對走**當前顯示模式解析後的名稱**：Original 比原名、MaskMiddle 比遮蔽名、
     CustomAlias 比別名、Anonymous 比動植物代號（查 `AnonymousIdentities` 既有指派，
     **只讀不指派**——沒被指派過的成員名稱搜不到是正確行為，指派只發生在訊息視窗端點）。
  3. 只搜文字訊息的 Text 欄（媒體訊息無文字可搜，檔名不搜——檔名未經遮蔽管線）。
- 效能：Text 欄無索引、LIKE 全掃——此資料量級（單機、萬則內）可接受；
  達到瓶頸再評估 FTS，先在文件註記。
- 測試：模式×名稱比對 4 條、遮蔽後門防堵 2 條、群組範圍/排序/上限 3 條，約 +9~10 條。

### 前端：搜尋面板＋跳轉＋高亮

1. **入口**：標頭 Aa 旁加 🔍 鈕，點擊展開/收合標頭下方的搜尋列
   （輸入框＋「本群組/全部群組」切換＋結果數）。
2. **結果清單**：搜尋列下方下拉面板列出結果（群組名、發言者、摘要——關鍵字以
   `<mark>` 高亮、時間）。高亮一律用 DOM 節點拆接組裝（比照 linkify），不走 innerHTML。
3. **點結果跳轉到上下文**：
   - 後端 `GET /api/groups/{id}/messages` 加 `aroundId=` 模式：回傳該訊息前後各半個
     天數視窗的訊息（實作上＝以該訊息時間為錨點開窗），含該訊息本身。
   - 前端：必要時先切群組，以 aroundId 視窗渲染，捲動到目標訊息並閃爍高亮
     （目標訊息背景短暫變色），視窗內所有符合關鍵字的文字也上 `<mark>`。
   - **與輪詢的相容性（本項最大風險點）**：跳轉後處於「歷史視窗」狀態，
     `pollNewer` 若照常 append 新訊息會在視窗尾端造成時間斷層。
     新增 `state.historicalView` 旗標：歷史視窗期間**暫停新訊息 append**
     （pending 內容狀態輪詢照常）；「回到最新」鈕常駐顯示，點擊走既有
     `selectGroup()` 完整重置（token 遞增天然丟棄過期回應，防競態機制不動）。
4. 關閉搜尋列＝清除高亮與結果；Esc 也可關閉。

---

## ⑥ 圖片檢視器：預設縮滿版面、點擊切換原尺寸

### 現況

Bootstrap modal（modal-lg）＋ `img-fluid`：大圖受限 modal 寬度、直角、超高圖會被裁切捲動，
沒有縮放互動。

### 方案（純前端）

1. Modal 改全螢幕燈箱樣式：深色滿版背景，圖片預設 **fit 模式**
   `max-width:100vw; max-height:100vh; object-fit:contain`——超出視窗的圖縮到剛好整個畫面，
   小於視窗的圖維持原尺寸不放大。
2. **左鍵點圖切換**：fit ↔ 原始大小（100%，容器 `overflow:auto` 可捲動看局部），
   游標 `zoom-in`/`zoom-out` 提示；圖片本身小於視窗時點擊不動作（沒東西可放大）。
3. 點圖片外（背景）關閉、Esc 關閉（Bootstrap 既有行為保留）。

---

## 影響評估

| 面向 | 影響 |
|---|---|
| DB / migration | 零改動（②刻意走 localStorage） |
| 收錄端 | 零改動 |
| 後端 Web | ⑤新增 search 端點＋`aroundId` 模式（`MessagesController`/新 DTO），其他項零改動 |
| 自動化測試 | ⑤約 +12 條（search 9~10、aroundId 2~3）；現 136 → 約 148+ |
| chat.js | ②變數化、③無、⑤搜尋面板/跳轉/高亮/`historicalView` 旗標——**唯一觸碰輪詢邏輯的點，體檢重點** |
| settings.js / Settings.cshtml | ②新增介面顯示卡片與同步邏輯 |
| chat.css / site.css | ①③④⑥樣式；②字級 calc 改寫（16 處納入 `--chat-base`） |
| 既有偏好 | `chat-font-size`（小/中/大）沿用；新增 `chat-font-base-px` |

## 實作階段（建議順序）

- **P1 視覺打磨**（①＋④）：token 化 gutter/radius、對齊、搜尋框膠囊、標頭底色。
- **P2 字級數值化**（②）：`--chat-base` 重構＋設定頁卡片＋設定頁自身同步。
- **P3 尾巴修正**（③）：依 P2 完成後的字級體系實測微調。
- **P4 圖片燈箱**（⑥）。
- **P5 搜尋後端**（⑤a）：search＋aroundId＋測試全綠。
- **P6 搜尋前端**（⑤b）：面板、跳轉、高亮、historicalView。
- **P7 全面體檢**：全字級×全功能回歸＋手機版＋136+新測試綠，推 dev。

## 決策點（預設採建議值，有意見再說）

1. **字級預設基準**：建議 20px（與現行「中」幾乎等大，無感遷移）；你若想預設更大可指定。
2. **①的參考圖**：未收到，依推斷規劃；實作前可補圖讓我對焦。
3. **④的解讀**：按「聊天標頭加底色」規劃；若指的是訊息上方發言者名字加底籤，成本相同、跟我說即可。
4. **搜尋上限**：100 筆、新→舊；不做分頁（要再翻通常代表該縮小關鍵字）。
