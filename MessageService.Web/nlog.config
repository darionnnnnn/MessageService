<?xml version="1.0" encoding="utf-8"?>
<nlog xmlns="http://www.nlog-project.org/schemas/NLog.xsd"
      xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
      autoReload="true"
      internalLogLevel="Warn"
      internalLogFile="${basedir}/logs/nlog-internal.log">

  <extensions>
    <add assembly="NLog.Web.AspNetCore" />
  </extensions>

  <targets>
    <!-- 服務本身的關鍵 log：收訊、下載、清除、檢視端存取、錯誤。用 ${basedir} 而非相對路徑——
         IIS in-process 代管時工作目錄通常可預期，但用 Task Scheduler 或其他方式啟動時就看
         排程設定的「起始位置」，容易找不到 log -->
    <target xsi:type="File" name="appFile"
            fileName="${basedir}/logs/messageservice-${shortdate}.log"
            layout="${longdate}|${level:uppercase=true}|${logger:shortName=true}|${message}${onexception:inner=|${exception:format=tostring}}"
            archiveEvery="Day"
            maxArchiveDays="30"
            encoding="utf-8" />

    <target xsi:type="Console" name="console"
            layout="${longdate}|${level:uppercase=true}|${logger:shortName=true}|${message}${onexception:inner=|${exception:format=tostring}}" />
  </targets>

  <rules>
    <!-- Microsoft/System 的雜訊只留 Warning 以上 -->
    <logger name="Microsoft.*" maxlevel="Info" final="true" />
    <logger name="System.Net.Http.*" maxlevel="Info" final="true" />

    <logger name="*" minlevel="Info" writeTo="appFile,console" />
  </rules>
</nlog>
